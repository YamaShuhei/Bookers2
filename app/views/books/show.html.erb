<h1>showページです</h1>

      <div class ="side-bar">
          <h2>New book</h2>
        <%= form_with model: @books do |f| %>
          <h4>Title</h4>
            <%= f.text_field :title %>
          <h4>Opinion</h4>
            <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
        </div>

    <h1>ページ1</h1>